# Optional: set to a local Hugging Face model ID or local path.
# Default (if omitted): Qwen/Qwen3-14B-Instruct
LOCAL_LLM_MODEL=Qwen/Qwen2.5-14B-Instruct
HF_TOKEN=example_hf_token

# Optional: token only needed to download gated/private models from HF.
# HF_TOKEN=your_hf_token_here